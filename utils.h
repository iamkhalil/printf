#ifndef UTILS_H
#define UTILS_H

#include <unistd.h>

#define CAPACITY 1024
#define FLUSHBUF (-1)

int _putchar(char c);
int _puts(char *s);
int _puts_without_newline(char *s);
unsigned int _strlen(const char *s);

#endif /* UTILS_H */
